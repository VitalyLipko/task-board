query GetProjectPageInfo($id: ID!) {
  project(id: $id) {
    id
    name
    tasks {
      id
      title
      created
      creator {
        id
        username
        fullName
      }
      labels {
        id
        backgroundColor
        title
      }
      assignees {
        id
        fullName
      }
    }
  }
}
