query GetTask($id: ID!) {
  task(id: $id) {
    id
    title
    description
    assignees {
      id
      fullName
    }
    created
    creator {
      id
      username
      fullName
    }
    isOpen
  }
}
