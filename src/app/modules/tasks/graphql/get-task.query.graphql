#import "./creator-fields.fragment.graphql"

query GetTask($id: ID!) {
  task(id: $id) {
    id
    title
    description
    assignees {
      id
      profile {
        fullName
      }
    }
    created
    creator {
      ...CreatorFields
    }
    status
    labels {
      id
      backgroundColor
      title
    }
    parentId
  }
}
