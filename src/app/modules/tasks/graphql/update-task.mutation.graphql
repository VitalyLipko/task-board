mutation UpdateTask($task: UpdateTaskInput!) {
  updateTask(task: $task) {
    id
    title
    description
    assignees {
      id
      fullName
    }
    labels {
      id
      title
      backgroundColor
    }
  }
}
